#include <bits/stdc++.h>

using namespace std; 

int main() 
{
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    int32_t n; cin >> n;
    cout << n << '\n';
    for (int i = 0; i < n; i++) cout << 1 << " ";
    return 0;
}