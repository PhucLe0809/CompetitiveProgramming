#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    int32_t test; cin >> test;
    int32_t n, m, z, t, col, row;
    while (test--)
    {
        cin >> n >> m >> z >> t;
        cout << 1 << " " << 1 << " " << n << " " << m;
        cout << '\n';  
    }
    return 0;
}